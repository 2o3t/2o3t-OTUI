export default {
    otDefaultColors(theme) {
        switch (theme) {
            case 'dark':
                return {
                    separator: {
                        normal: [ 'grey-f' ],
                    },
                };
            case 'light':
            default:
                return {
                    separator: {
                        normal: [ 'grey-f' ],
                    },
                };
        }
    },

};
