export default {
    otDefaultColors(theme) {
        switch (theme) {
            case 'dark':
                return {
                    normal: [ 'danger-f' ],
                };
            case 'light':
            default:
                return {
                    normal: [ 'danger-f' ],
                };
        }
    },
};
