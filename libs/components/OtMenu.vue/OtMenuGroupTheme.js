export default {
    otDefaultColors(theme) {
        switch (theme) {
            case 'dark':
                return {
                    normal: [ 'grey-f' ],
                };
            case 'light':
            default:
                return {
                    normal: [ 'grey-f' ],
                };
        }
    },
};
