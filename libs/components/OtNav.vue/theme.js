export default {
    otDefaultColors(theme) {
        switch (theme) {
            case 'dark':
                return {
                };
            case 'light':
            default:
                return {
                };
        }
    },
};
